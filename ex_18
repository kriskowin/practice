lines = int(input())
matrix = []

for line in range(lines):
    data = []
    current_line = input().split(", ")
    for element in current_line:
        if int(element) % 2 == 0:
            data.append(int(element))
    matrix.append(data)

print(matrix)
